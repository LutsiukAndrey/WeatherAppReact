import { QuoteAuthor, QuoteText, SectionQuote } from './Quote.module';

export const Quote = () => {
  return (
    <SectionQuote>
      <QuoteText>
        Who cares about the clouds when we're together? Just sing a song and
        bring the sunny weather.
      </QuoteText>
      <QuoteAuthor>Dale Evans</QuoteAuthor>
    </SectionQuote>
  );
};
